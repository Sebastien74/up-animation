{%- trans_default_domain 'admin' -%}

<li>
    <a class="as-arrow" href="javascript:void(0)" aria-expanded="false">
        <span class="btn-icon small me-3"><i class="icm-sign-out-alt"></i></span>{{ "Logs"|trans|raw }}
    </a>
    <ul aria-expanded="false" class="collapse">
        <li>
            <a href="{{ path('admin_logs') }}">
                <span class="hide-menu">{{ "Tout afficher"|trans|raw }}</span>
            </a>
        </li>
        <li>
            <a class="swal-delete-link" href="{{ path('admin_log_clear') }}">
                <span class="hide-menu">{{ "Supprimer"|trans|raw }}</span>
            </a>
        </li>
    </ul>
</li>