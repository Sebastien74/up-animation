{%- trans_default_domain 'admin' -%}

{%- set args = {website: websiteId(), (interface['name']): entity.id} -%}
{%- if interface.masterField is defined and interface.masterField != 'configuration' and interface.masterField != 'website' -%}
    {%- set args = args|merge({(interface.masterField): interface.masterFieldId}) -%}
{%- endif -%}
{%- set args = ('admin_'~interface['name']~'_position')|routeArgs(interface.entity, args) -%}

{%- if not ajaxView -%}
    <span class="position" data-bs-toggle="tooltip" title="{{"Modifier la position"|trans|striptags }}">
        <a href="#" data-bs-toggle="modal" data-bs-target="#modal-position-{{ entity.id }}">
            {{ entity.position }}
        </a>
    </span>
{%- endif -%}

{{ form_start(form, {attr: {id: "modal-position-form-"~entity.id, class: 'mb-0', action: path("admin_"~interface.name~"_position", args)}}) }}
    <div class="modal fade modal-position{% if ajaxView %} d-block show{% endif %}" id="modal-position-{{ entity.id }}" tabindex="-1" role="dialog"
         aria-labelledby="modal-position-{{ entity.id }}-label" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered modal-md" role="document">
            <div class="modal-content border">
                <div class="modal-header flex-column align-items-center text-center border-0 pb-0">
                    <span class="modal-icon w-100">
                        <span class="icon-wrap">
                            <i class="icm-sort-numeric-down-alt"></i>
                        </span>
                    </span>
                    <h2 id="modal-position-{{ entity.id }}-label" class="modal-title fw-bold mt-3 mb-0">{{ "Modifier la position"|trans|raw }}</h2>
                    <button type="button" class="btn-close btn-icon position-absolute top-0 end-0 m-3" data-bs-dismiss="modal" aria-label="{{ "Fermer"|trans|raw }}">
                        <i class="icm-times"></i>
                    </button>
                </div>
                <div class="modal-body px-3 px-md-5">
                    <div class="row">
                        <div class="col-12 col-md-6 offset-md-3">
                            {{ form_widget(form.position) }}
                        </div>
                    </div>
                </div>
                <div class="modal-footer border-0 justify-content-center flex-wrap gap-2 pt-0">
                    <button type="button" class="btn-dismiss btn-app muted d-inline-flex align-items-center gap-2" data-bs-dismiss="modal">
                        <i class="icm-ban"></i><span>{{ "Annuler"|trans|raw }}</span>
                    </button>
                    {{ form_widget(form.save) }}
                </div>
            </div>
        </div>
    </div>
{{ form_end(form) }}