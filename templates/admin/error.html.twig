{%- trans_default_domain 'admin' -%}

<section id="errorStatus" class="mt-4">
    <div id="row">
        <div class="col-lg-4">
            {%- include 'bundles/TwigBundle/Exception/messages.html.twig' -%}
            <a href="{{ path('admin_dashboard', {website: websiteId}) }}" class="btn-app mt-4">
                <i class="icm-reply"></i>
                {{ "Retourner au tableau de bord"|trans|raw }}
            </a>
        </div>
    </div>
</section>