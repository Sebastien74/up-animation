{% trans_default_domain 'admin' %}

<div class="card">
    <h4 class="card-title card-header"><i class="icm-at me-2"></i>{{ "Emails"|trans }}</h4>
    <div class="card-body pb-0">
        <div class="row">
            {{ form_row(form.configuration.emailsDev) }}
            {{ form_row(form.configuration.emailsSupport) }}
        </div>
    </div>
</div>