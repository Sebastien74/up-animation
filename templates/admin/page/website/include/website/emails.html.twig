{%- trans_default_domain 'admin' -%}

<div class="card mb-3">
    <div class="card-body pb-2">
        <h4 class="main-subtitle"><i class="icm-at"></i>{{ "Emails"|trans|raw }}</h4>
        <div class="row">
            {{ form_row(form.configuration.emailsDev) }}
            {{ form_row(form.configuration.emailsSupport) }}
        </div>
    </div>
</div>
