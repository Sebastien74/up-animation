<?xml version="1.0" encoding="utf-8"?>
<browserconfig>
    <msapplication>
        <tile>
            {% set color = 'favicon'|color(website, 'browserconfig') %}
            {%- if color and color.color != 'browserconfig' -%}
                <TileColor>{{ color.color }}</TileColor>
            {%- endif -%}
        </tile>
    </msapplication>
</browserconfig>