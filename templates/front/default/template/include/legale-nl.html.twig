<h2 class="mb-4">Definities</h2>
<p><b>Klant:</b> elke professional of natuurlijke persoon die handelingsbekwaam is in de zin van de artikelen 1123 en volgende van het Burgerlijk Wetboek, of elke rechtspersoon, die de Website bezoekt waarop deze algemene voorwaarden van toepassing zijn.<br><b>Diensten en Service:</b> <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> stelt aan Klanten ter beschikking:</p>
<p><b>Inhoud:</b> alle elementen waaruit de op de Website aanwezige informatie bestaat, met name teksten – afbeeldingen – video’s.</p>
<p><b>Klantinformatie:</b> hierna aangeduid als “Informatie(n)”, zijnde alle persoonsgegevens die door <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> kunnen worden bewaard voor het beheer van uw account, het beheer van de klantrelatie en voor analyse- en statistische doeleinden.</p>
<p><b>Gebruiker:</b> internetgebruiker die verbinding maakt met en gebruikmaakt van de hierboven genoemde website.</p>
<p><b>Persoonsgegevens:</b> “De informatie die, in welke vorm dan ook, rechtstreeks of onrechtstreeks, de identificatie mogelijk maakt van de natuurlijke personen op wie zij betrekking heeft” (artikel 4 van wet nr. 78-17 van 6 januari 1978).</p>
<p>De begrippen “persoonsgegevens”, “betrokkene”, “verwerker” en “bijzondere categorieën van gegevens” hebben de betekenis zoals gedefinieerd in de Algemene Verordening Gegevensbescherming (AVG: nr. 2016-679).</p>
<h2 class="my-4 d-inline-block w-100">1. Voorstelling van de website.</h2>
<p>Krachtens artikel 6 van wet nr. 2004-575 van 21 juni 2004 betreffende het vertrouwen in de digitale economie, wordt aan de gebruikers van de website <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> de identiteit meegedeeld van de verschillende betrokkenen bij de realisatie en de opvolging ervan:</p>
<p><strong>Eigenaar</strong>: {% if companyName %}{{ companyName|raw }}{% else %}<span class="text-danger bold">Maatschappelijke naam</span>{% endif %}{% if capital %} met een maatschappelijk kapitaal van {{ capital|raw }}{% endif %}{% if vatNumber %} - BTW-nummer: {{ vatNumber|raw }}{% endif %}{% if siretNumber %} - SIRET-nummer: {{ siretNumber|raw }}{% endif %}{% if commercialRegisterNumber %} - Handelsregisternummer: {{ commercialRegisterNumber|raw }}{% endif %}{% if companyAddress %} - {{ companyAddress|raw }}{% endif %}<br><strong>Verantwoordelijke voor publicatie</strong>: {% if managerName %}{{ managerName|raw }}{% else %}<span class="text-danger bold">Naam van de verantwoordelijke voor publicatie</span>{% endif %}{% if managerEmail %}{% if managerName %} - {% endif %}{{ managerEmail|raw }}{% else %}<span class="text-danger bold">E-mailadres van de verantwoordelijke voor publicatie</span>{% endif %}<br>De verantwoordelijke voor publicatie is een natuurlijke persoon of een rechtspersoon.<br><strong>Webmaster</strong>: {% if webmasterName %}{{ webmasterName|raw }}{% else %}<span class="text-danger bold">Naam van de webmaster</span>{% endif %}{% if webmasterEmail %}{% if webmasterName %} - {% endif %}{{ webmasterEmail|raw }}{% endif %}<br><strong>Hostingprovider</strong>: {% if hostName %}{{ hostName|raw }}{% else %}<span class="text-danger bold">Naam van de hostingprovider</span>{% endif %}<br>{% if hostAddress %}{{ hostAddress|raw }}{% else %}<span class="text-danger bold">Adres van de hostingprovider</span>{% endif %}<br><strong>Functionaris voor gegevensbescherming</strong>: {% if protectionOfficerName %}{{ protectionOfficerName|raw }}{% else %}<span class="text-danger bold">Naam van de functionaris voor gegevensbescherming</span>{% endif %}{% if protectionOfficerEmail %}{% if protectionOfficerName %} - {% endif %}{{ protectionOfficerEmail|raw }}{% else %}<span class="text-danger bold">E-mailadres van de functionaris</span>{% endif %}<br></p>
<h2 class="my-4 d-inline-block w-100">2. Algemene gebruiksvoorwaarden van de website en de aangeboden diensten.</h2>
<p>De Website vormt een geestelijk werk dat wordt beschermd door de bepalingen van de Franse Intellectuele Eigendomscode en door de toepasselijke internationale regelgeving. De Klant mag op geen enkele wijze, geheel of gedeeltelijk, elementen of werkzaamheden van de Website hergebruiken, overdragen of exploiteren voor eigen rekening.</p>
<p>Het gebruik van de website <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> impliceert de volledige en onvoorwaardelijke aanvaarding van de hieronder beschreven gebruiksvoorwaarden. Deze gebruiksvoorwaarden kunnen op elk moment worden gewijzigd of aangevuld; gebruikers van de website <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> worden daarom uitgenodigd om ze regelmatig te raadplegen.</p>
<p>Deze website is normaal gezien te allen tijde toegankelijk voor gebruikers. Een onderbreking wegens technisch onderhoud kan echter worden beslist door <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a>, die zich dan zal inspannen om de gebruikers vooraf de data en tijdstippen van de interventie mee te delen. De website <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> wordt regelmatig bijgewerkt door <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> in haar hoedanigheid van verantwoordelijke. Evenzo kunnen de wettelijke vermeldingen op elk moment worden gewijzigd: zij blijven niettemin bindend voor de gebruiker, die wordt uitgenodigd er zo vaak mogelijk naar te verwijzen om kennis ervan te nemen.</p>
<h2 class="my-4 d-inline-block w-100">3. Beschrijving van de geleverde diensten.</h2>
<p>De website <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> heeft tot doel informatie te verstrekken over alle activiteiten van de onderneming. <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> streeft ernaar op de website <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> zo nauwkeurig mogelijke informatie te verstrekken. Zij kan echter niet aansprakelijk worden gesteld voor omissies, onjuistheden of tekortkomingen in de actualisering, ongeacht of deze haar te wijten zijn of aan derde partners die haar dergelijke informatie verstrekken.</p>
<p>Alle op de website <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> vermelde informatie wordt louter ter indicatie gegeven en kan evolueren. Bovendien zijn de op de website <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> opgenomen gegevens niet uitputtend. Zij worden verstrekt onder voorbehoud van wijzigingen die sinds de online publicatie werden aangebracht.</p>
<h2 class="my-4 d-inline-block w-100">4. Contractuele beperkingen inzake technische gegevens.</h2>
<p>De website maakt gebruik van JavaScript-technologie. De website kan niet aansprakelijk worden gesteld voor materiële schade die verband houdt met het gebruik van de website. Bovendien verbindt de gebruiker zich ertoe de website te bezoeken met recente apparatuur die geen virussen bevat en met een browser van de laatste generatie die up-to-date is. De website <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> wordt gehost bij een dienstverlener op het grondgebied van de Europese Unie, overeenkomstig de bepalingen van de Algemene Verordening Gegevensbescherming (AVG: nr. 2016-679).</p>
<p>Het doel is een dienstverlening te bieden die de best mogelijke toegankelijkheid garandeert. De hostingprovider verzekert de continuïteit van zijn dienst 24 uur per dag, elke dag van het jaar. Hij behoudt zich echter het recht voor de hostingdienst voor zo kort mogelijke duur te onderbreken, met name voor onderhoud, verbetering van zijn infrastructuur, storingen in zijn infrastructuur, of wanneer de Diensten en Service een als abnormaal beschouwd verkeer genereren.</p>
<p><a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> en de hostingprovider kunnen niet aansprakelijk worden gesteld bij storingen van het internetnetwerk, de telefoonlijnen of computer- en telecommunicatieapparatuur, met name als gevolg van netwerkcongestie die de toegang tot de server verhindert.</p>
<h2 class="my-4 d-inline-block w-100">5. Intellectuele eigendom en namaak.</h2>
<p><a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> is houder van de intellectuele eigendomsrechten en beschikt over de gebruiksrechten op alle elementen die op de website toegankelijk zijn, met name teksten, afbeeldingen, grafieken, logo’s, video’s, pictogrammen en geluiden. Elke reproductie, voorstelling, wijziging, publicatie of aanpassing, geheel of gedeeltelijk, van de elementen van de website, ongeacht het gebruikte middel of procedé, is verboden, behalve met voorafgaande schriftelijke toestemming van: <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a>.</p>
<p>Elke niet-toegestane exploitatie van de website of van een van de daarin opgenomen elementen zal worden beschouwd als namaak en vervolgd overeenkomstig de bepalingen van de artikelen L.335-2 en volgende van de Franse Intellectuele Eigendomscode.</p>
<h2 class="my-4 d-inline-block w-100">6. Beperking van aansprakelijkheid.</h2>
<p><a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> treedt op als uitgever van de website. <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> is verantwoordelijk voor de kwaliteit en de juistheid van de Inhoud die zij publiceert.</p>
<p><a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> kan niet aansprakelijk worden gesteld voor directe en indirecte schade aan de apparatuur van de gebruiker, bij de toegang tot de website <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a>, die voortvloeit uit het gebruik van apparatuur die niet voldoet aan de in punt 4 vermelde specificaties, of uit het optreden van een bug of een incompatibiliteit.</p>
<p><a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> kan evenmin aansprakelijk worden gesteld voor indirecte schade (zoals bijvoorbeeld verlies van markt of verlies van een kans) die het gevolg is van het gebruik van de website <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a>. Interactieve ruimtes (mogelijkheid om vragen te stellen via het contactgedeelte) staan ter beschikking van gebruikers. <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> behoudt zich het recht voor, zonder voorafgaande ingebrekestelling, elke inhoud te verwijderen die in deze ruimte wordt geplaatst en die in strijd is met de in Frankrijk toepasselijke wetgeving, met name de bepalingen inzake gegevensbescherming. In voorkomend geval behoudt <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> zich ook het recht voor om de burgerlijke en/of strafrechtelijke aansprakelijkheid van de gebruiker in te roepen, met name bij een bericht van racistische, beledigende, lasterlijke of pornografische aard, ongeacht het gebruikte medium (tekst, foto…).</p>
<h2 class="my-4 d-inline-block w-100">7. Beheer van persoonsgegevens.</h2>
<p>De Klant wordt geïnformeerd over de regelgeving inzake marketingcommunicatie, de wet van 21 juni 2014 betreffende het vertrouwen in de digitale economie, de Wet Informatique et Libertés van 6 augustus 2004, evenals de Algemene Verordening Gegevensbescherming (AVG: nr. 2016-679).</p>
<h3 class="mt-1 mb-3 d-inline-block w-100">7.1 Verantwoordelijken voor het verzamelen van persoonsgegevens</h3>
<p>Voor de Persoonsgegevens die worden verzameld in het kader van de aanmaak van het persoonlijke account van de Gebruiker en zijn/haar navigatie op de Website, wordt de verwerkingsverantwoordelijke vertegenwoordigd door {% if companyRepresentativeName %}{{ companyRepresentativeName|raw }}{% else %}<span class="text-danger bold">Naam van de wettelijke vertegenwoordiger van de onderneming</span>{% endif %}, als wettelijke vertegenwoordiger.</p>
<p>Als verwerkingsverantwoordelijke van de verzamelde gegevens verbindt <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> zich ertoe het kader van de geldende wettelijke bepalingen na te leven. Het is met name aan de Klant om de doeleinden van zijn gegevensverwerkingen vast te stellen, zijn prospects en klanten – vanaf het verzamelen van hun toestemming – volledige informatie te verstrekken over de verwerking van hun persoonsgegevens en een register van verwerkingen bij te houden dat overeenstemt met de werkelijkheid. Telkens wanneer <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> Persoonsgegevens verwerkt, neemt <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> alle redelijke maatregelen om ervoor te zorgen dat de Persoonsgegevens accuraat en relevant zijn met betrekking tot de doeleinden waarvoor <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> ze verwerkt.</p>
<h3 class="mt-1 mb-3 d-inline-block w-100">7.2 Doeleinden van de verzamelde gegevens</h3>
<p><a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> kan geheel of gedeeltelijk de volgende gegevens verwerken:</p>
<ul><li>om navigatie op de Website mogelijk te maken en het beheer en de traceerbaarheid van door de gebruiker bestelde diensten en service te verzekeren: verbindings- en gebruiksgegevens van de Website, facturatie, bestelgeschiedenis, enz.;</li><li>om computerfraude te voorkomen en te bestrijden (spamming, hacking…): gebruikte computerapparatuur voor de navigatie, IP-adres, wachtwoord (gehasht);</li><li>om navigatie op de Website te verbeteren: verbindings- en gebruiksgegevens;</li><li>om optionele tevredenheidsonderzoeken uit te voeren op <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a>: e-mailadres;</li><li>om communicatiecampagnes te voeren (sms, e-mail): telefoonnummer, e-mailadres.</li></ul>
<p><a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> verhandelt uw persoonsgegevens niet; deze worden dus uitsluitend gebruikt uit noodzaak of voor statistische en analysetoepassingen.</p>
<h3 class="mt-1 mb-3 d-inline-block w-100">7.3 Recht op inzage, rectificatie en bezwaar</h3>
<p>Overeenkomstig de geldende Europese regelgeving beschikken de Gebruikers van <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> over de volgende rechten:</p>
<ul><li>recht op inzage (artikel 15 AVG) en rectificatie (artikel 16 AVG), actualisering, volledigheid van gebruikersgegevens; recht op afscherming of wissing van persoonsgegevens van gebruikers (artikel 17 AVG) wanneer deze onjuist, onvolledig, dubbelzinnig, verouderd zijn, of wanneer de verzameling, het gebruik, de mededeling of de bewaring ervan verboden is;</li><li>recht om te allen tijde toestemming in te trekken (artikel 13-2c AVG);</li><li>recht op beperking van de verwerking (artikel 18 AVG);</li><li>recht van bezwaar tegen de verwerking (artikel 21 AVG);</li><li>recht op gegevensoverdraagbaarheid van gegevens die gebruikers hebben verstrekt wanneer deze geautomatiseerd worden verwerkt op basis van hun toestemming of een overeenkomst (artikel 20 AVG);</li><li>recht om de bestemming van gebruikersgegevens na hun overlijden te bepalen en te kiezen aan wie <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> de gegevens al dan niet mag meedelen aan een derde die zij vooraf hebben aangewezen.</li></ul>
<p>Zodra <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> kennis heeft van het overlijden van een Gebruiker en bij gebrek aan instructies van zijn/haar kant, verbindt <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> zich ertoe zijn/haar gegevens te vernietigen, tenzij het bewaren ervan noodzakelijk is voor bewijsdoeleinden of om te voldoen aan een wettelijke verplichting.</p>
<p>Indien de Gebruiker wil weten hoe <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> zijn/haar Persoonsgegevens gebruikt, vraagt om rectificatie of bezwaar maakt tegen de verwerking, kan de Gebruiker <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> schriftelijk contacteren op het volgende adres:</p>
<p>ONDERNEMING – DPO, {% if protectionOfficerName %}{{ protectionOfficerName|raw }}{% else %}<span class="text-danger bold">Naam van de functionaris voor gegevensbescherming</span>{% endif %}{% if protectionOfficerAddress %}{% if protectionOfficerName %} <br> {% endif %}{{ protectionOfficerAddress|raw }}{% else %} <span class="text-danger bold">Adres van de functionaris</span>{% endif %}</p>
<p>In dat geval moet de Gebruiker aangeven welke Persoonsgegevens hij/zij wil dat <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> corrigeert, actualiseert of verwijdert, waarbij hij/zij zich nauwkeurig identificeert met een kopie van een identiteitsbewijs (identiteitskaart of paspoort).</p>
<p>Verzoeken tot verwijdering van Persoonsgegevens zijn onderworpen aan de verplichtingen die <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> door de wet worden opgelegd, met name inzake bewaring of archivering van documenten. Tot slot kunnen de Gebruikers van <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> een klacht indienen bij de toezichthoudende autoriteiten, met name bij de CNIL (<a href="https://www.cnil.fr/fr/plaintes" aria-label="CNIL">https://www.cnil.fr/fr/plaintes</a>).</p>
<h3 class="mt-1 mb-3 d-inline-block w-100">7.4 Geen doorgifte van persoonsgegevens</h3>
<p><a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> verbiedt zichzelf de Informatie die over haar Klanten werd verzameld te verwerken, te hosten of over te dragen naar een land buiten de Europese Unie of dat door de Europese Commissie als “niet passend” wordt erkend, zonder de klant vooraf te informeren. Niettemin blijft <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> vrij in de keuze van haar technische en commerciële onderaannemers, op voorwaarde dat zij voldoende waarborgen bieden met betrekking tot de vereisten van de Algemene Verordening Gegevensbescherming (AVG: nr. 2016-679).</p>
<p><a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> verbindt zich ertoe alle nodige voorzorgsmaatregelen te nemen om de veiligheid van de Informatie te waarborgen en in het bijzonder te voorkomen dat deze aan niet-geautoriseerde personen wordt meegedeeld. Indien echter een incident dat de integriteit of vertrouwelijkheid van de Informatie van de Klant aantast ter kennis komt van <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a>, zal deze de Klant zo spoedig mogelijk informeren en hem/haar meedelen welke corrigerende maatregelen werden genomen. Bovendien verzamelt <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> geen “bijzondere categorieën van gegevens”.</p>
<p>De Persoonsgegevens van de Gebruiker kunnen worden verwerkt door dochterondernemingen van <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> en door verwerkers (dienstverleners), uitsluitend om de doeleinden van dit beleid te realiseren.</p>
<p>Binnen de grenzen van hun respectieve bevoegdheden en voor de hierboven vermelde doeleinden, zijn de voornaamste personen die toegang kunnen hebben tot de gegevens van de Gebruikers van <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> voornamelijk de medewerkers van onze klantendienst.</p>
<h3 class="mt-1 mb-3 d-inline-block w-100">7.5 Soorten verzamelde gegevens</h3>
<p>Met betrekking tot gebruikers van een website <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> verzamelen wij de volgende gegevens die onmisbaar zijn voor de werking van de dienst en die gedurende maximaal 9 maanden na het einde van de contractuele relatie worden bewaard:<br>Welke gegevens worden verzameld</p>
<p><a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> verzamelt bovendien informatie die de gebruikerservaring verbetert en het mogelijk maakt om contextuele adviezen aan te bieden:<br>Gegevens om de gebruikerservaring te verbeteren en contextuele adviezen aan te bieden</p>
<p>Deze gegevens worden gedurende maximaal 9 maanden na het einde van de contractuele relatie bewaard</p>
<h2 class="my-4 d-inline-block w-100">8. Melding van incidenten</h2>
<p>Ondanks alle inspanningen is geen enkele methode van gegevensoverdracht via internet en geen enkele methode van elektronische opslag volledig veilig. Bijgevolg kunnen wij geen absolute veiligheid garanderen. Indien wij kennis zouden nemen van een beveiligingslek, zouden wij de betrokken gebruikers informeren zodat zij passende maatregelen kunnen nemen. Onze procedures voor incidentmelding houden rekening met onze wettelijke verplichtingen, zowel nationaal als Europees. Wij verbinden ons ertoe onze klanten volledig te informeren over alle kwesties die verband houden met de beveiliging van hun account en hen alle nodige informatie te verschaffen om hen te helpen voldoen aan hun eigen rapporteringsverplichtingen op regelgevend vlak.</p>
<p>Geen enkele persoonlijke informatie van de gebruiker van de website <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> wordt gepubliceerd zonder medeweten van de gebruiker, uitgewisseld, overgedragen, afgestaan of verkocht op welke drager dan ook aan derden. Enkel in het geval van een overname van <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> en haar rechten zou overdracht van genoemde informatie aan een eventuele koper mogelijk zijn, die op zijn beurt aan dezelfde verplichting tot bewaring en wijziging van de gegevens tegenover de gebruiker van de website <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> gebonden zou zijn.</p>
<h3 class="mt-1 mb-3 d-inline-block w-100">Beveiliging</h3>
<p>Om de veiligheid en vertrouwelijkheid van Persoonsgegevens en Gezondheidsgegevens te waarborgen, maakt <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> gebruik van netwerken die worden beschermd door standaardmaatregelen zoals firewalls, pseudonimisering, encryptie en wachtwoordbeveiliging.</p>
<p>Bij de verwerking van Persoonsgegevens neemt <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> alle redelijke maatregelen om deze te beschermen tegen verlies, misbruik, ongeoorloofde toegang, bekendmaking, wijziging of vernietiging.</p>
<h2 class="my-4 d-inline-block w-100">9. Hyperlinks “cookies” en internet-“tags”</h2>
<p>De website <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> bevat een aantal hyperlinks naar andere websites, die met toestemming van <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> werden geplaatst. <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> heeft echter niet de mogelijkheid om de inhoud van aldus bezochte websites te controleren en aanvaardt daarvoor bijgevolg geen aansprakelijkheid.</p>
<p>Tenzij u beslist cookies uit te schakelen, aanvaardt u dat de website ze kan gebruiken. U kunt deze cookies op elk moment uitschakelen, gratis, via de hieronder herinnerde uitschakelmogelijkheden, met dien verstande dat dit de toegankelijkheid tot alle of een deel van de door de website aangeboden Diensten en Service kan verminderen of verhinderen.</p>
<h3 class="mt-1 mb-3 d-inline-block w-100">9.1. “COOKIES”</h3>
<p>Een “cookie” is een klein informatiebestand dat naar de browser van de Gebruiker wordt gestuurd en op het toestel van de Gebruiker wordt opgeslagen (bv.: computer, smartphone) (hierna “Cookies”). Dit bestand bevat informatie zoals de domeinnaam van de Gebruiker, de internetprovider van de Gebruiker, het besturingssysteem van de Gebruiker, evenals datum en uur van toegang. Cookies kunnen in geen geval het toestel van de Gebruiker beschadigen.</p>
<p><a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> kan informatie van de Gebruiker verwerken met betrekking tot zijn/haar bezoek aan de Website, zoals bezochte pagina’s en uitgevoerde zoekopdrachten. Deze informatie stelt <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> in staat de Inhoud van de Website en de navigatie van de Gebruiker te verbeteren.</p>
<p>Cookies die de navigatie en/of het leveren van de door de Website aangeboden diensten vergemakkelijken, geven de Gebruiker de mogelijkheid zijn/haar browser zo in te stellen dat hij/zij kan beslissen of hij/zij ze al dan niet wil aanvaarden, zodat Cookies op het toestel worden opgeslagen of, integendeel, worden geweigerd, hetzij systematisch, hetzij afhankelijk van hun uitgever. De Gebruiker kan zijn/haar browser eveneens zo instellen dat de aanvaarding of weigering van Cookies hem/haar van tijd tot tijd wordt voorgesteld, voordat een Cookie op het toestel kan worden opgeslagen. <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> informeert de Gebruiker dat in dat geval mogelijk niet alle functionaliteiten van de browser beschikbaar zijn.</p>
<p>Indien de Gebruiker het opslaan van Cookies op zijn/haar toestel of browser weigert, of indien de Gebruiker de daar opgeslagen Cookies verwijdert, wordt de Gebruiker geïnformeerd dat zijn/haar navigatie en ervaring op de Website beperkt kunnen zijn. Dit kan ook het geval zijn wanneer <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> of een van haar dienstverleners, voor technische compatibiliteitsdoeleinden, het type browser dat door het toestel wordt gebruikt, de taal- en weergave-instellingen of het land van waaruit het toestel met internet verbonden lijkt te zijn, niet kan herkennen.</p>
<p>In voorkomend geval wijst <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> elke aansprakelijkheid af voor de gevolgen die verband houden met een verminderde werking van de Website en van eventueel door <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> aangeboden diensten, als gevolg van (i) de weigering van Cookies door de Gebruiker (ii) de onmogelijkheid voor <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> om de Cookies die nodig zijn voor hun werking te bewaren of te raadplegen als gevolg van de keuze van de Gebruiker. Voor het beheer van Cookies en de keuzes van de Gebruiker verschilt de configuratie per browser. Deze wordt beschreven in het helpmenu van de browser, waarin wordt uitgelegd hoe de Gebruiker zijn/haar wensen met betrekking tot Cookies kan wijzigen.</p>
<p>De Gebruiker kan op elk moment zijn/haar wensen met betrekking tot Cookies kenbaar maken en wijzigen. <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> kan bovendien een beroep doen op externe dienstverleners om haar te helpen de in deze sectie beschreven informatie te verzamelen en te verwerken.</p>
<p>Ten slotte kunnen Twitter, Facebook, Linkedin en Google Plus eveneens cookies op uw toestellen (computer, tablet, mobiele telefoon) plaatsen door te klikken op de pictogrammen van deze sociale netwerken op de Website van <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> of in haar mobiele applicatie, indien de Gebruiker het plaatsen van cookies heeft aanvaard door zijn/haar navigatie op de Website of de mobiele applicatie van <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> voort te zetten.</p>
<p>Dit type cookies wordt alleen op uw toestellen geplaatst op voorwaarde dat u ermee instemt, door uw navigatie op de Website of de mobiele applicatie van <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> voort te zetten. De Gebruiker kan echter op elk moment terugkomen op zijn/haar toestemming voor het plaatsen van dit type cookies door <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a>.</p>
<h3 class="mt-1 mb-3 d-inline-block w-100">Artikel 9.2. INTERNET-“TAGS”</h3>
<p><a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> kan af en toe internet-tags (ook “tags” genoemd, of actietags, 1-pixel GIF’s, transparante GIF’s, onzichtbare GIF’s en 1-op-1 GIF’s) gebruiken en deze inzetten via een partner die gespecialiseerd is in webanalyses en die zich mogelijk (en dus de corresponderende informatie, inclusief het IP-adres van de Gebruiker, opslaat) in een buitenland bevindt.</p>
<p>Deze tags worden zowel geplaatst in online advertenties die internetgebruikers toegang geven tot de Website, als op de verschillende pagina’s ervan.</p>
<p>Deze technologie stelt <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> in staat de reacties van bezoekers op de Website en de doeltreffendheid van haar acties te evalueren (bijvoorbeeld het aantal keren dat een pagina wordt geopend en de geraadpleegde informatie), evenals het gebruik van deze Website door de Gebruiker.</p>
<p>De externe dienstverlener kan eventueel informatie verzamelen over de bezoekers van de Website en van andere websites dankzij deze tags, rapporten opstellen over de activiteit van de Website ten behoeve van <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a>, en andere diensten leveren met betrekking tot het gebruik ervan en van internet.</p>
<h2 class="my-4 d-inline-block w-100">10. Toepasselijk recht en bevoegde rechtbank.</h2>
<p>Elk geschil in verband met het gebruik van de website <a href="{{ app.request.getSchemeAndHttpHost() }}" aria-label="{{ companyName|striptags }}">{{ app.request.getSchemeAndHttpHost() }}</a> is onderworpen aan het Franse recht. Buiten de gevallen waarin de wet dit niet toestaat, wordt exclusieve bevoegdheid toegekend aan de bevoegde rechtbanken van Plaats van de bevoegde rechtbank in geval van geschil.</p>